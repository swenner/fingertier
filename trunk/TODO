TODO:

  * create psyeudo random and a sorted playlist files on startup (or demand)
  * save playlist as efficient file (just the paths?)
  * implement saving of the configuration
  * should pause on call
  * should prevent the phone from sleeping while playing
  * 'Aux' button should play next song if phone is locked
  * configuration menu
  * library creation progress bar. GUI should never get locked.


GTK layout:

+------------------------+
|                        |
|       cover image      |
|                        |
+------------------------+
|         artist         |
|         title          |
|         album          |
+------------------------+
|         3/120          |
+------------------------+
| prev, play/pause, next |
+------------------------+